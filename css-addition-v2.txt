
/* ---------- eBay live card images (added) ---------- */
/* Reserved image box: always the same size, even when a listing has no photo */
.deal-img-wrap{
  width:100%;
  aspect-ratio:1/1;
  background:var(--paper);
  border-bottom:1.5px solid var(--brick);
  overflow:hidden;
}
.deal-img{
  width:100%;
  height:100%;
  object-fit:cover;
  display:block;
}
/* Locks every title to exactly 2 lines of height, so cards don't grow/shrink
   based on how long the real eBay title happens to be */
.deal-title{
  display:-webkit-box;
  -webkit-line-clamp:2;
  -webkit-box-orient:vertical;
  overflow:hidden;
  min-height:2.6em;
  line-height:1.3em;
}
